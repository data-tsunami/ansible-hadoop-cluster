---

- name: Hadoop master node setup
  hosts: master
  sudo: true

  roles:
    - server
    - master

- name: Hadoop slave nodes setup
  hosts: slave
  sudo: true

  roles:
    - server
    - slave
